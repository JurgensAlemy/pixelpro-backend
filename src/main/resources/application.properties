# Configuracion de PostgreSQL
# La nueva URL. El puerto estandar de Postgres es 5432.
# Spring Boot intentara conectarse a una base de datos llamada "pixelpro".
spring.datasource.url=jdbc:postgresql://localhost:5432/pixelpro
# Tu usuario y contrasena de Postgres (cambialos si son diferentes)
# Los valores mas comunes son "postgres" y tu contrasena.
spring.datasource.username=postgres
spring.datasource.password=postgres
# El nuevo "idioma" (Dialecto)
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# Estas lineas siguen igual
spring.application.name=pixelpro
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.generate-ddl=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=America/Lima
# ========================================
# AUTENTICACION JWT (Stateless)
# ========================================
# La autenticacion ahora usa JWT Bearer Tokens (sin sesiones)
# La clave secreta debe ser larga y generada de forma aleatoria (base64 o hexadecimal)
jwt.secret-key=i31UeHSVFatzftXz4OwCrZkWZRROJkgrvAHY1cHGlqm/QO8ZnO5vWRSR96Qz/uVIjx5Y8Fjou0XLxHlD6aYXmA==
# Tiempo de expiración en milisegundos (24 horas)
jwt.expiration-time=86400000
# ========================================
# SESIONES (OBSOLETO - NO SE USA)
# ========================================
# Las siguientes propiedades ya no se usan porque ahora usamos JWT stateless
# Se mantienen por compatibilidad pero pueden eliminarse
server.servlet.session.cookie.domain=localhost
server.servlet.session.cookie.path=/
server.servlet.session.cookie.same-site=Lax
server.servlet.session.cookie.secure=false
# ========================================
# UPLOADS
# ========================================
app.upload.dir.products=uploads/products/